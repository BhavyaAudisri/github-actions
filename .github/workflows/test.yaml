name: build and deploy
on:
  pull_request:
    types:
      - opened
      - synchronize
jobs:
  CLONE:
    runs-on: ubuntu-latest
    steps:
      - name: checkout scm
        if: github.ref_name == 'feature' && github.actor == 'ashishaluwala-25'
        run: echo "this is feature"
  
