name: build and deploy
on:
  pull_request:
    types:
      - opened
      - synchronize
jobs:
  CLONE:
    runs-on: ubuntu-latest
    steps:
      - name: checkout scm
        run: echo "this is main"
     
